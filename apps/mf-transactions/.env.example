# Copy to .env.local for `npm run dev -w apps/mf-transactions` (or set this
# in your hosting provider's project settings for `next build`/`next start`).

# Base URL of the real tech-challenge-2 API, used by the /api/transactions
# Route Handlers (packages/shared/src/apiClient.ts) — never hardcoded.
API_BASE_URL=https://tech-challenge-api.example.com
